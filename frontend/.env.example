# Backend API base URL (must be running)
VITE_API_URL=http://localhost:3000/api

# Mock mode: set to 'true' for frontend-only / live deployment (no backend)
# Enables demo login (admin@rms.local / admin123) with mock data
# VITE_MOCK_MODE=true
